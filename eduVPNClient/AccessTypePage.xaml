<Page x:Class="eduVPNClient.AccessTypePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignWidth="298"
      d:DesignHeight="373"
      Title="Access Type">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button
            Grid.Row="0"
            Style="{StaticResource eduVPNChoiceButtonStyle}"
            IsEnabled="False">Secure Internet</Button>

        <TextBlock
            Grid.Row="0"
            Style="{StaticResource eduVPNChoiceLabelStyle}">Connect to the internet securely, <LineBreak/>
            for example when using public WiFi.</TextBlock>

        <Button
            Grid.Row="1"
            Style="{StaticResource eduVPNChoiceButtonStyle}"
            Command="NavigationCommands.GoToPage"
            CommandTarget="{Binding ElementName=NavigationFrame}"
            CommandParameter="InstanceSelectionPage.xaml">Institute Access</Button>

        <TextBlock
            Grid.Row="1"
            Style="{StaticResource eduVPNChoiceLabelStyle}">Access your organization network</TextBlock>
    </Grid>
</Page>
