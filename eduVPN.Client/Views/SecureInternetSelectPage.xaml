<view:InstanceSelectPage
    x:Class="eduVPN.Views.SecureInternetSelectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:res="clr-namespace:eduVPN.Client.Resources"
    xmlns:view="clr-namespace:eduVPN.Views"
    mc:Ignorable="d"
    d:DesignWidth="298"
    d:DesignHeight="373"
    Title="{x:Static res:Strings.SecureInternetSelectPageTitle}"
    Style="{StaticResource eduVPNConnectWizardPageWithBack}">

    <ListBox
        Grid.Row="1"
        Style="{StaticResource eduVPNListStyle}"
        ItemTemplate="{StaticResource eduVPNInstanceSelectionItemTemplate}"
        ItemContainerStyle="{StaticResource eduVPNListBoxItemStyle}"
        ItemsSource="{Binding InstanceList}"
        SelectedItem="{Binding SelectedInstance}"
        SelectionChanged="InstanceList_SelectionChanged"/>
</view:InstanceSelectPage>
