<!--
    eduVPN - End-user friendly VPN

    Copyright: 2017, The Commons Conservancy eduVPN Programme
    SPDX-License-Identifier: GPL-3.0+
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:glob="clr-namespace:System.Globalization;assembly=mscorlib"
    xmlns:model="clr-namespace:eduVPN.Models;assembly=eduVPN"
    xmlns:panels="clr-namespace:eduVPN.Views.Panels"
    xmlns:controls="clr-namespace:eduVPN.Views.Controls;assembly=eduVPN.Views"
    xmlns:res="clr-namespace:eduVPN.Client.Resources">

    <DataTemplate x:Key="eduVPNInstanceSelectionItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="43"/>
            </Grid.RowDefinitions>

            <Rectangle
                Grid.ColumnSpan="2"
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <controls:ImageWithFallback
                Grid.Column="0"
                Grid.Row="1"
                UriSource="{Binding Path=Logo}"
                UriFallbackSource="pack://application:,,,/Resources/GenericInstance.png"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="0,5,5,5"
                RenderOptions.BitmapScalingMode="Fant"/>

            <Image
                Grid.Column="0"
                Grid.Row="1"
                Margin="2"
                MaxHeight="12"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Source="{Binding Path=DataContext.Wizard.ActiveSession.State, Converter={StaticResource VPNSessionStatusTypeIconConverter}, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                ToolTip="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                AutomationProperties.Name="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                RenderOptions.BitmapScalingMode="Fant">
                <Image.Visibility>
                    <MultiBinding
                        Converter="{StaticResource ConnectingInstanceEqualVisibilityConverter}"
                        FallbackValue="{x:Static Visibility.Collapsed}">
                        <Binding/>
                        <Binding
                            Path="DataContext.Wizard.ActiveSession.ConnectingProfile.Instance"
                            RelativeSource="{RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}"/>
                    </MultiBinding>
                </Image.Visibility>
            </Image>

            <TextBlock
                Grid.Column="1"
                Grid.Row="1"
                Margin="5,5,0,5"
                Text="{Binding Path=DisplayName}"
                Style="{StaticResource eduVPNListBoxItemTextStyle}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="eduVPNProfileSelectionItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="43"/>
            </Grid.RowDefinitions>

            <Rectangle
                Grid.ColumnSpan="3"
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <Image
                Grid.Column="0"
                Grid.Row="1"
                MaxHeight="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Source="{Binding Path=DataContext.Wizard.ActiveSession.State, Converter={StaticResource VPNSessionStatusTypeIconConverter}, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                ToolTip="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                AutomationProperties.Name="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                RenderOptions.BitmapScalingMode="Fant">
                <Image.Visibility>
                    <MultiBinding
                        Converter="{StaticResource ConnectingProfileEqualVisibilityConverter}"
                        FallbackValue="{x:Static Visibility.Collapsed}">
                        <Binding/>
                        <Binding
                            Path="DataContext.Wizard.ActiveSession.ConnectingProfile"
                            RelativeSource="{RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}"/>
                    </MultiBinding>
                </Image.Visibility>
            </Image>

            <TextBlock
                Grid.Column="1"
                Grid.Row="1"
                Margin="-55,5"
                HorizontalAlignment="Center"
                Text="{Binding Path=DisplayName}"
                Style="{StaticResource eduVPNListBoxItemTextStyle}"/>

            <ContentControl
                Grid.Column="2"
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="5,5,0,5"
                ContentTemplate="{StaticResource eduVPNTwoFactorAuthenticationIcon}"
                Visibility="{Binding Path=IsTwoFactorAuthentication, Converter={StaticResource TwoFactorAuthenticationIconVisibilityConverter}, FallbackValue={x:Static Visibility.Collapsed}}"
                ToolTip="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                AutomationProperties.Name="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                IsTabStop="False"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="eduVPNCompactProfileSelectionItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="32"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>

            <Rectangle
                Grid.ColumnSpan="3"
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <Image
                Grid.Column="0"
                Grid.Row="1"
                MaxHeight="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Source="{Binding Path=DataContext.Wizard.ActiveSession.State, Converter={StaticResource VPNSessionStatusTypeIconConverter}, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                ToolTip="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                AutomationProperties.Name="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                RenderOptions.BitmapScalingMode="Fant">
                <Image.Visibility>
                    <MultiBinding
                        Converter="{StaticResource ConnectingProfileEqualVisibilityConverter}"
                        FallbackValue="{x:Static Visibility.Collapsed}">
                        <Binding/>
                        <Binding
                            Path="DataContext.Wizard.ActiveSession.ConnectingProfile"
                            RelativeSource="{RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}"/>
                    </MultiBinding>
                </Image.Visibility>
            </Image>

            <TextBlock
                Grid.Column="1"
                Grid.Row="1"
                Margin="-32,0"
                HorizontalAlignment="Center"
                Text="{Binding Path=DisplayName}"
                Style="{StaticResource eduVPNListBoxItemTextStyle}"/>

            <ContentControl
                Grid.Column="2"
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                ContentTemplate="{StaticResource eduVPNTwoFactorAuthenticationIcon}"
                Visibility="{Binding Path=IsTwoFactorAuthentication, Converter={StaticResource TwoFactorAuthenticationIconVisibilityConverter}, FallbackValue={x:Static Visibility.Collapsed}}"
                ToolTip="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                AutomationProperties.Name="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                IsTabStop="False"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="eduVPNInstanceProfileSelectionItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="43"/>
            </Grid.RowDefinitions>

            <Rectangle
                Grid.ColumnSpan="3"
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <controls:ImageWithFallback
                Grid.Column="0"
                Grid.Row="1"
                UriSource="{Binding Path=Instance.Logo}"
                UriFallbackSource="pack://application:,,,/Resources/GenericInstance.png"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="0,5,5,5"
                RenderOptions.BitmapScalingMode="Fant"/>

            <Image
                Grid.Column="0"
                Grid.Row="1"
                Margin="2"
                MaxHeight="12"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Source="{Binding Path=DataContext.Wizard.ActiveSession.State, Converter={StaticResource VPNSessionStatusTypeIconConverter}, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                ToolTip="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                AutomationProperties.Name="{Binding Path=DataContext.Wizard.ActiveSession.StateDescription, RelativeSource={RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}}"
                RenderOptions.BitmapScalingMode="Fant">
                <Image.Visibility>
                    <MultiBinding
                        Converter="{StaticResource ConnectingProfileEqualVisibilityConverter}"
                        FallbackValue="{x:Static Visibility.Collapsed}">
                        <Binding/>
                        <Binding
                            Path="DataContext.Wizard.ActiveSession.ConnectingProfile"
                            RelativeSource="{RelativeSource AncestorType={x:Type panels:ConnectingSelectPanel}}"/>
                    </MultiBinding>
                </Image.Visibility>
            </Image>

            <Grid
                Grid.Column="1"
                Grid.Row="1"
                Margin="5,5,-55,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock
                    Margin="0,0,0,-1"
                    Text="{Binding Path=DisplayName}"
                    Style="{StaticResource eduVPNTextBlockStyle}"/>

                <TextBlock
                    Grid.Row="1"
                    Margin="0,-1,0,0"
                    Text="{Binding Path=Instance.DisplayName}"
                    FontSize="10"
                    Style="{StaticResource eduVPNTextBlockStyle}"/>
            </Grid>

            <ContentControl
                Grid.Column="2"
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="5,5,0,5"
                ContentTemplate="{StaticResource eduVPNTwoFactorAuthenticationIcon}"
                Visibility="{Binding Path=IsTwoFactorAuthentication, Converter={StaticResource TwoFactorAuthenticationIconVisibilityConverter}, FallbackValue={x:Static Visibility.Collapsed}}"
                ToolTip="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                AutomationProperties.Name="{x:Static res:Strings.TwoFactorAuthenticationRequired}"
                IsTabStop="False"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type model:MessageNotification}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Rectangle
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <TextBlock
                Grid.Row="1"
                Margin="0,5,0,1"
                Style="{StaticResource eduVPNInfoTitleTextBlockStyle}"
                Text="{x:Static res:Strings.MessageNotificationTitle}"/>

            <TextBlock
                Grid.Row="2"
                Margin="0,1,0,5"
                Style="{StaticResource eduVPNMessageBodyStyle}"
                Text="{Binding Path=Text}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type model:MessageOfTheDay}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Rectangle
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <TextBlock
                Grid.Row="1"
                Margin="0,5"
                Style="{StaticResource eduVPNMessageBodyStyle}"
                Text="{Binding Path=Text}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type model:MessageMaintenance}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Rectangle
                Grid.ColumnSpan="2"
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <TextBlock
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Margin="0,5,0,1"
                Style="{StaticResource eduVPNInfoTitleTextBlockStyle}"
                Text="{x:Static res:Strings.MessageMaintenanceTitle}"/>

            <TextBlock
                Grid.Row="2"
                Margin="0,1,2,0"
                Style="{StaticResource eduVPNInfoLabelTextBlockStyle}"
                Text="{x:Static res:Strings.MessageMaintenanceBegin}"/>

            <TextBlock
                Grid.Row="2"
                Grid.Column="1"
                Margin="2,1,0,0"
                Style="{StaticResource eduVPNInfoDataBlockStyle}"
                Text="{Binding Path=Begin, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}}"/>

            <TextBlock
                Grid.Row="3"
                Margin="0,0,2,1"
                Style="{StaticResource eduVPNInfoLabelTextBlockStyle}"
                Text="{x:Static res:Strings.MessageMaintenanceEnd}"/>

            <TextBlock
                Grid.Row="3"
                Grid.Column="1"
                Margin="2,0,0,1"
                Style="{StaticResource eduVPNInfoDataBlockStyle}"
                Text="{Binding Path=End, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}}"/>

            <TextBlock
                Grid.Row="4"
                Grid.ColumnSpan="2"
                Margin="0,1,0,5"
                Style="{StaticResource eduVPNMessageBodyStyle}"
                Text="{Binding Path=Text}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="eduVPNSelectionItemTemplate">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Rectangle
                Style="{StaticResource eduVPNListBoxItemVisualSeparatorStyle}"/>

            <TextBlock
                Grid.Column="1"
                Grid.Row="1"
                Margin="0,5"
                HorizontalAlignment="Center"
                Text="{Binding}"
                Style="{StaticResource eduVPNListBoxItemTextStyle}"/>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
